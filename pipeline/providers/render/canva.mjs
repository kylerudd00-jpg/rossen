export async function renderWithCanva() {
  throw new Error(
    "Canva Autofill rendering is not wired in yet. This path requires Canva Enterprise credentials, brand template setup, and async job polling.",
  );
}
